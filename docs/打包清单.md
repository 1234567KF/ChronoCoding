# AI编程智驾 - 打包清单

> **版本**：v1.3
>
> 将此文件夹复制到新电脑环境后，让 AI 阅读本清单和主手册即可自动完成所有配置。

---

## 文件清单

### 主文件（必选）

| 文件路径 | 说明 |
|----------|------|
| `MANUAL.md` | **主手册**，供用户阅读 |
| `INSTALL.md` | **AI 安装指南**，供 AI 阅读并完整执行 |
| `AICoding.md` | **单文件入口**，放入 AI IDE 即可自动安装 |
| `打包清单.md` | 本文件，说明各文件用途 |

### 模板文件（templates/）

配置模板，用于快速初始化各工具：

| 文件路径 | 说明 | 目标位置 |
|----------|------|----------|
| `templates/settings.json.template` | Claude Code 配置 | `~/.claude/settings.json` |
| `templates/config.yaml.template` | ruflo 全局配置 | `~/.claude-flow/config.yaml` |
| `templates/tdd-config.yaml.template` | TDD 测试配置 | `~/.claude-flow/tdd-config.yaml` |
| `templates/pre-commit.template` | Git pre-commit hook | `~/.claude-flow/hooks/pre-commit` |
| `templates/pipeline-example.md` | Pipeline 配置示例 | 参考用于 `implementation-plan.md` |
| `templates/wiki-template.md` | Wiki 生成模板 | 参考用于项目 Wiki 生成 |

### 技能文件（.claude/skills/）

项目技能全部在 `.claude/skills/` 目录下，kf- 前缀为团队自建技能：

| 文件路径 | 说明 |
|----------|------|
| `.claude/skills/kf-spec/SKILL.md` | Spec 驱动开发 |
| `.claude/skills/kf-code-review-graph/SKILL.md` | 代码审查依赖图谱 |
| `.claude/skills/kf-web-search/SKILL.md` | 多引擎智能搜索 |
| `.claude/skills/kf-browser-ops/SKILL.md` | 浏览器自动化操作 |
| `.claude/skills/kf-multi-team-compete/SKILL.md` | 多团队竞争评审 |
| `.claude/skills/kf-alignment/SKILL.md` | 对齐工作流 |
| `.claude/skills/kf-model-router/SKILL.md` | 模型智能路由 |
| `.claude/skills/kf-prd-generator/SKILL.md` | PRD 文档生成 |
| `.claude/skills/kf-triple-collaboration/SKILL.md` | 三方协作 |
| `.claude/skills/kf-ui-prototype-generator/SKILL.md` | UI 原型生成 |
| `.claude/skills/kf-skill-design-expert/SKILL.md` | Skill 设计专家 |
| `.claude/skills/gspowers/` | SOP 导航（上游） |
| `.claude/skills/gstack/` | 产品流程（上游） |

### 补丁文件（gspowers-pipeline-patch/）

当需要启用 **gspowers Pipeline 多模块流水线开发** 时使用：

| 文件路径 | 说明 |
|----------|------|
| `gspowers-pipeline-patch/pipeline.md` | 多模块流水线开发规范 |
| `gspowers-pipeline-patch/execute-patch.md` | execute.md 修改说明 |
| `gspowers-pipeline-patch/install-pipeline.ps1` | 自动安装脚本 |

---

## 模板使用说明

### 1. settings.json.template（Claude Code 配置）

```powershell
# 复制到目标位置
Copy-Item templates\settings.json.template "$env:USERPROFILE\.claude\settings.json"

# 替换 Token 占位符
# 将 YOUR_TOKEN_HERE 替换为你的实际 Token
```

**必填字段**：
- `ANTHROPIC_AUTH_TOKEN`: 你的 API Token

**可选字段**：
- `model`: 模型名称（默认 MiniMax-M2.7）
- `theme`: 界面主题（light/dark）

### 2. config.yaml.template（ruflo 全局配置）

```powershell
# 创建目录
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\.claude-flow"

# 复制配置
Copy-Item templates\config.yaml.template "$env:USERPROFILE\.claude-flow\config.yaml"
```

### 3. tdd-config.yaml.template（TDD 配置）

```powershell
# 创建目录
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\.claude-flow"

# 复制配置
Copy-Item templates\tdd-config.yaml.template "$env:USERPROFILE\.claude-flow\tdd-config.yaml"
```

### 4. pre-commit.template（Git Hooks）

```bash
# 创建目录
mkdir -p ~/.claude-flow/hooks

# 复制并添加执行权限
cp templates/pre-commit.template ~/.claude-flow/hooks/pre-commit
chmod +x ~/.claude-flow/hooks/pre-commit
```

### 5. pipeline-example.md（Pipeline 配置示例）

此文件作为参考，复制到 `implementation-plan.md` 中的 `pipeline.modules` 部分即可使用。

### 6. wiki-template.md（Wiki 生成模板）

此文件作为参考，AI 在执行"生成 Wiki"时会参考此模板格式。

---

## 快速开始

### 在新环境执行

```
1. 下载 AICoding.md 放入 AI IDE
2. 对 AI 说"执行安装"
3. AI 自动完成所有安装和配置
4. 用户只需配置 Token（唯一需要手动介入的步骤）
```

### 完整执行流程

```
AI 读取 AICoding.md
    ↓
阶段0：平台与网络检测
    ↓
阶段1：安装基础工具（Git/Node.js/Claude Code）
    ↓
阶段2：下载项目仓库（git clone AICoding）
    ↓
阶段3：按 INSTALL.md 完成安装（ruflo/MCP/RTK/gspowers）
    ↓
阶段4：验证安装
    ↓
完成！用户可以开始使用
```

---

## 可选：安装 Pipeline 扩展

如果需要使用 **多模块流水线开发** 功能：

```
1. 让 AI 阅读 gspowers-pipeline-patch/install-pipeline.ps1
2. 或让 AI 根据 execute-patch.md 手动修改
3. 参考 templates/pipeline-example.md 配置模块
```

---

## 完整目录结构

```
AI编程智驾/
├── README.md                   # 项目入口
├── AICoding.md                 # 单文件入口
├── CHANGELOG.md                # 版本记录
├── CONTRIBUTING.md             # 贡献指南
├── LICENSE                     # MIT 许可证
│
├── docs/                       # 文档目录
│   ├── MANUAL.md               # 完整使用手册
│   ├── INSTALL.md              # AI 安装指南
│   ├── 打包清单.md              # 本文件
│
├── .claude/                    # Claude Code 项目配置
│   ├── CLAUDE.md              # 项目指令
│   ├── settings.json          # 项目配置
│   └── skills/                # 技能目录
│       ├── kf-spec/    # Spec 驱动开发
│       ├── kf-code-review-graph/ # 代码审查图谱
│       ├── kf-web-search/     # 多引擎搜索
│       ├── kf-browser-ops/    # 浏览器自动化
│       ├── kf-multi-team-compete/ # 多团队竞争
│       ├── kf-alignment/      # 对齐工作流
│       ├── kf-model-router/   # 模型路由
│       ├── kf-prd-generator/  # PRD 生成
│       ├── kf-triple-collaboration/ # 三方协作
│       ├── kf-ui-prototype-generator/ # UI 原型
│       ├── kf-skill-design-expert/ # Skill 设计
│       ├── gspowers/          # SOP 导航（上游）
│       └── gstack/            # 产品流程（上游）
│
├── templates/                  # 配置模板目录
│   ├── settings.json.template
│   ├── config.yaml.template
│   ├── tdd-config.yaml.template
│   ├── pre-commit.template
│   ├── pipeline-example.md
│   └── wiki-template.md
│
└── gspowers-pipeline-patch/    # Pipeline 扩展补丁
    ├── pipeline.md
    ├── execute-patch.md
    └── install-pipeline.ps1
```

---

## 版本信息

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.4 | 2026-04-28 | 新增 prd-generator 技能（SDD Excel → PRD 生成） |
| v1.3 | 2026-04-27 | 新增 RTK Token 节省、TDD 扩展、Pipeline 多模块流水线、模板目录 |
| v1.2 | - | 完善触发词表 |
| v1.1 | - | 初始版本 |

---

## 注意事项

1. **Token 配置**：唯一需要用户手动介入的步骤。使用 `templates/settings.json.template` 时记得替换 `YOUR_TOKEN_HERE`
2. **superpowers**：需要在 Claude Code 中执行 `/plugin install superpowers@claude-plugins-official`
3. **技能扩充**：用户需要说"允许 AI 自动扩充技能"才会安装 TDD/Wiki/Pipeline 扩展
4. **RTK**：需要 GitHub 网络访问，如遇问题请配置镜像源
5. **模板文件**：所有模板使用**相对路径**引用，确保在新环境中可直接使用

---

## 快速触发词

| 触发词 | 功能 | 来源 |
|--------|------|------|
| `/gspowers` | 启动 SOP 流程导航 | gspowers |
| `triple [任务]` | 通用三方协作 | kf-triple-collaboration |
| `TDD` | 启用测试先行模式 | 扩展 |
| `/夯 [任务]` | 多团队竞争评审 | kf-multi-team-compete |
| `/review-graph` | 代码审查依赖图谱 | kf-code-review-graph |
| `/web-search [问题]` | 多引擎智能搜索 | kf-web-search |
| `/browser-ops` | 浏览器自动化操作 | kf-browser-ops |
| `spec coding` | Spec 驱动开发 | kf-spec |
| `/对齐` | 对齐工作流 | kf-alignment |
| `模型路由` | 模型智能路由 | kf-model-router |
